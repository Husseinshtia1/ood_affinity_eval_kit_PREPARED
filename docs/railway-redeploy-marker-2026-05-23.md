# Railway redeploy marker

This marker forces Railway to deploy a snapshot that includes:

- `start.sh`
- `requirements.txt`
- Railway Railpack fallback support

If Railway build logs still do not show these files, reconnect the GitHub source or verify the service root directory is the repository root.
